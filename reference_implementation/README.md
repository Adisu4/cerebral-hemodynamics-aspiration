# Archived result-producing implementation

These five Python files are byte-for-byte copies of the source whose SHA-256
hashes are embedded in the saved simulation results. Their historical filenames
are intentionally unchanged. Do not edit this directory.

The installable implementation in `src/cerebral_hemodynamics_aspiration/`
contains the maintained scientific modules. Tests compare them directly with
this snapshot.

This snapshot supports provenance of the computational outputs. It does not
establish biological validity, clinical efficacy, or safety.
